#!/bin/bash -eux

# pushd /vagrant/configs/dockerImages/jenkins
# docker build -t registry.service.lidop.local:5000/lidop/jenkins:latest .
# popd

# pushd /vagrant/configs/dockerImages/nginx
# docker build -t registry.service.lidop.local:5000/lidop/nginx:latest .
# popd

# pushd /vagrant/configs/dockerImages/gitbucket
# docker build -t registry.service.lidop.local:5000/lidop/gitbucket:latest .
# popd

# pushd /vagrant/configs/dockerImages/backup
# docker build -t registry.service.lidop.local:5000/lidop/backup:latest .
# popd

# pushd /vagrant/configs/dockerImages/serverspec
# docker build -t registry.service.lidop.local:5000/lidop/serverspec:latest .
# # popd